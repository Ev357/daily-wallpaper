import { View, Text } from "react-native";
import { UText } from "@/components/ui/text";

const EventScreen = () => {
  return (
    <View>
      <UText>EventScreen</UText>
    </View>
  );
};

export default EventScreen;
